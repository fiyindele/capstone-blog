<%- include("partials/header.ejs") %>

<body>
  <h1>Enter your blog post below 👇</h1>
  <form action="/submit" method="POST">
    <div><input type="text" name="title" placeholder="Title ..." /></div>
    <div>
      <input type="text" name="content" placeholder="And it goes like ..." />
    </div>
    <input type="submit" value="Enter" />
  </form>

  <section>
    <div class="column card-style">
      <div class="card-text">
        <% if (locals.blogHeader) { %>
        <h1>Title: <%= blogHeader %></h1>
        <% } %> <% if (locals.blogContent) { %>
        <p class="ellipsis"><%= blogContent %></p>
        <% } %>
      </div>
    </div>
  </section>
</body>

<%- include("partials/footer.ejs") %>
